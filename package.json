{
  "name": "server",
  "version": "1.0.0",
  "description": "Fast-Food-Fast​ is a food delivery service app for a restaurant where you can place your order and get your food as soon as possible",
  "main": "server.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "cross-env NODE_ENV=test && nyc --reporter=html --reporter=text mocha --require babel-register --timeout 10000 ./test/*.js --exit",
    "start": "babel-node ./server.js",
    "start:dev": "nodemon --watch server --exec babel-node ./server.js",
    "test:dev": "nyc --reporter=html --reporter=text mocha --compilers js:babel-register ./test --timeout 20000 --exit",
    "coveralls": "nyc npm test&& nyc report --reporter=text-lcov | coveralls",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "author": "Nwabuzor C Obiora",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "chai": "^4.1.2",
    "eslint-config-airbnb": "^17.1.0",
    "express": "^4.16.3",
    "mocha": "^5.2.0",
    "morgan": "^1.9.1",
    "supertest": "^3.3.0",
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.1",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-polyfill": "^6.26.0",
    "babel-preset-airbnb": "^2.5.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "eslint": "^5.5.0",
    "eslint-config-airbnb": "^17.1.0",
    "eslint-plugin-import": "^2.14.0",
    "jsdoc": "^3.5.5",
    "nyc": "^13.0.1"
  }
}
